import AcmeLogo from "@/app/ui/acme-logo";
import { inter } from"@/app/ui/fonts";

export default function Page() {
  return <>
    <main className={`${inter} antialiased flex min-h-screen flex-col `}>
      Dashboard Page
    </main>
  </>
}